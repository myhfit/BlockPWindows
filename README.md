# Introduce
BlockWindows is sub project for BlockP Projects. 

# Requirements
Need FFM(Compile Time), Compile level=1.8

# Features  
## Format
>pe(exe/dll)  
## Tool
>prevent sleep(win)

## Function
>support set window dark mode()
>provide os handler to list process/systemmonitor
